import React from 'react'
import {QuizQues} from '../Types/Type'
export type Props={
    questions: string,
    options: string[]
}

export const QuizCard:React.FC<QuizQues> = ({questions,options}) => {
    return (
        <div>
         
        </div>
    )
}
